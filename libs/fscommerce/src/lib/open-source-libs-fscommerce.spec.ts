import { openSourceLibsFscommerce } from './open-source-libs-fscommerce';

describe('openSourceLibsFscommerce', () => {
  it('should work', () => {
    expect(openSourceLibsFscommerce()).toEqual('open-source-libs-fscommerce');
  });
});
