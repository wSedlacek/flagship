import { openSourceLibsFsengagement } from './open-source-libs-fsengagement';

describe('openSourceLibsFsengagement', () => {
  it('should work', () => {
    expect(openSourceLibsFsengagement()).toEqual(
      'open-source-libs-fsengagement'
    );
  });
});
