import { openSourceLibsFscomponents } from './open-source-libs-fscomponents';

describe('openSourceLibsFscomponents', () => {
  it('should work', () => {
    expect(openSourceLibsFscomponents()).toEqual(
      'open-source-libs-fscomponents'
    );
  });
});
