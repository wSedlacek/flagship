import { openSourceLibsFsengage } from './open-source-libs-fsengage';

describe('openSourceLibsFsengage', () => {
  it('should work', () => {
    expect(openSourceLibsFsengage()).toEqual('open-source-libs-fsengage');
  });
});
