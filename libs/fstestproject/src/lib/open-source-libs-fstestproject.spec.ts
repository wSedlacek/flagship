import { openSourceLibsFstestproject } from './open-source-libs-fstestproject';

describe('openSourceLibsFstestproject', () => {
  it('should work', () => {
    expect(openSourceLibsFstestproject()).toEqual(
      'open-source-libs-fstestproject'
    );
  });
});
