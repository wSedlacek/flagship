import { openSourceLibsFscheckout } from './open-source-libs-fscheckout';

describe('openSourceLibsFscheckout', () => {
  it('should work', () => {
    expect(openSourceLibsFscheckout()).toEqual('open-source-libs-fscheckout');
  });
});
