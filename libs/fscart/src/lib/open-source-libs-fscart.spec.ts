import { openSourceLibsFscart } from './open-source-libs-fscart';

describe('openSourceLibsFscart', () => {
  it('should work', () => {
    expect(openSourceLibsFscart()).toEqual('open-source-libs-fscart');
  });
});
