import { openSourceLibsFsproductindex } from './open-source-libs-fsproductindex';

describe('openSourceLibsFsproductindex', () => {
  it('should work', () => {
    expect(openSourceLibsFsproductindex()).toEqual(
      'open-source-libs-fsproductindex'
    );
  });
});
