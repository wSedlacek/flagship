import { openSourceLibsFslocator } from './open-source-libs-fslocator';

describe('openSourceLibsFslocator', () => {
  it('should work', () => {
    expect(openSourceLibsFslocator()).toEqual('open-source-libs-fslocator');
  });
});
