import { openSourceLibsFsapp } from './open-source-libs-fsapp';

describe('openSourceLibsFsapp', () => {
  it('should work', () => {
    expect(openSourceLibsFsapp()).toEqual('open-source-libs-fsapp');
  });
});
