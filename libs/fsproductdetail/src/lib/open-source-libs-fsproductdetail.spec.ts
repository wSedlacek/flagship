import { openSourceLibsFsproductdetail } from './open-source-libs-fsproductdetail';

describe('openSourceLibsFsproductdetail', () => {
  it('should work', () => {
    expect(openSourceLibsFsproductdetail()).toEqual(
      'open-source-libs-fsproductdetail'
    );
  });
});
