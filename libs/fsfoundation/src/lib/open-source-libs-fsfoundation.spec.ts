import { openSourceLibsFsfoundation } from './open-source-libs-fsfoundation';

describe('openSourceLibsFsfoundation', () => {
  it('should work', () => {
    expect(openSourceLibsFsfoundation()).toEqual(
      'open-source-libs-fsfoundation'
    );
  });
});
